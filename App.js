/* eslint-disable react-native/no-inline-styles */
import React from 'react';
import { Text, View } from 'react-native';

const HellowordApp = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: 'center',
        AllignItems: 'Center',
      }}>
      <Text> Helle, World how are you doing </Text>
    </View>
  );
};
export default HellowordApp;
