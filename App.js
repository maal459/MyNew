/* eslint-disable react-native/no-inline-styles */
import React from 'react';
import { Text, View } from 'react-native';

const HellowordApp = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: 'centr',
        AllignItems: 'Center',
      }}>
      <Text> Helle, World </Text>
    </View>
  );
};
export default HellowordApp;
